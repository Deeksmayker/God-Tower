using UnityEngine;

public class EyeEnemy : MonoBehaviour{
	/*
	[SerializeField] private float damage;
	private void OnTriggerEnter(Collider col){
		var victim = GetComponentInChildren<ITakeHit>();
		var player = GetComponentInParent<PlayerUnit>();
		if (player && victim != null){
			victim.TakeHit(damage, transform.position, "Eye sees");
		}
	}
	*/
}
